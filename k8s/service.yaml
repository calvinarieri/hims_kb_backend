apiVersion: v1
kind: Service
metadata:
  name: hims-kb-service
  namespace: hims-kb
  labels:
    app: hims-kb-backend
    app.kubernetes.io/name: hims-kb
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: hims-kb-backend
